section .text:
    extern strrev ; (char*) -> char*
    extern int_abs ; (i32) -> i32
    extern itoa ; (char* buf, u64 len, i32 val, bool is_signed) -> char*
    extern print ; (char*)
    extern printi ; (i32)

